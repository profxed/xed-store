[WebService]
# Let Cockpit trust the proxy’s TLS indication and client IPs
ProtocolHeader = X-Forwarded-Proto
ForwardedForHeader = X-Forwarded-For

# Serve plain HTTP behind the proxy (avoids double TLS + redirects)
AllowUnencrypted = true

# (Usually not needed on Umbrel; include only if you proxy under a subpath)
# UrlRoot = /apps/xed-cockpit

# (Optional) Allow cross-domain websocket origins if you hit issues
# Origins = https://umbrel.local http://umbrel.local
